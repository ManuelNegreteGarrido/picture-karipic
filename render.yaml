databases:
  - name: postgres
    ipAllowList: []
    databaseName: Picture_development
    user: userdb

services:
  - type: web
    name: Picture_development
    env: ruby
    buildCommand: "./render-build.sh"
    startCommand: "bundle exec rails s"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: postgres
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false